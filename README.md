# 🖼️ Асинхронный Обработчик Изображений (FastAPI + Celery + Redis)

## 🎯 Ключевые достижения и Техническая Архитектура

Проект демонстрирует реализацию **масштабируемой, асинхронной архитектуры микросервисов**, где веб-сервер не блокируется длительными операциями, делегируя их фоновым рабочим процессам.

### 1. Стек и Асинхронная Разработка
* **Backend / API**: Разработан на **FastAPI**, используя принципы асинхронного программирования. API принимает запрос, немедленно возвращает **`task_id`** и диспетчеризирует задачу.
* **Асинхронные Задачи**: **Celery Worker** выполняет ресурсоемкие операции по обработке изображений (**Pillow**) в отдельном процессе.
* **Брокер Задач**: **Redis** используется как высокоскоростной брокер сообщений для Celery, а также как бэкенд для хранения результатов задач.

### 2. Мульти-сервисная Контейнеризация (Docker)
Проект оркестрован через **Docker Compose** и состоит из пяти независимых сервисов, демонстрируя полный цикл работы асинхронной системы:

| Сервис | Технология | Назначение |
| :--- | :--- | :--- |
| **`api`** | FastAPI / Python | Прием запросов, диспетчеризация задач. |
| **`worker`** | Celery Worker / Python | Выполнение ресурсоемких задач обработки изображений. |
| **`redis`** | Redis | Брокер сообщений для Celery и бэкенд для хранения результатов. |
| **`flower`** | Flower | Мониторинг и визуализация статуса задач Celery в реальном времени. |
| **`frontend`** | Nginx | Обслуживание статического демонстрационного интерфейса. |

### 3. Функциональность и Обработка Изображений
Сервис предоставляет эндпоинты для следующих операций:
* **Запуск задач**: `/crop`, `/grayscale`, `/resize` (мгновенно возвращают `task_id`).
* **Мониторинг**: `/task-status/{task_id}` (проверка статуса).
* **Получение результата**: `/result/{task_id}` (выдача обработанного файла).

### 4. Полный Конвейер CI/CD (GitHub Actions)
Настроен полностью автоматизированный конвейер с использованием **GitHub Actions**, который гарантирует качество кода и готовность к развертыванию:
* **Job 'test'**: Автоматически запускает **Pytest** в контейнере `api` при каждом пуше. Использует **MagicMock** для изоляции и тестирования логики API и Celery-вызовов без фактического выполнения фоновых задач.
* **Job 'build_push'**: **(CD)** После успешного тестирования, Job выполняет вход в Docker Hub, собирает **единый образ приложения** (используется для `api` и `worker`) и публикует его в реестр.

### 5. Готовность к Развертыванию
Конвейер полностью готов к финальной стадии CD. Благодаря публикации унифицированного образа в Docker Hub, обновление на удаленном сервере сводится к запуску команд `docker compose pull / up`.

---

## ⚙️ Как запустить локально
Для запуска проекта достаточно наличия **Docker** и **Docker Compose**.
1.  Клонируйте репозиторий.
2.  Запустите все сервисы в фоновом режиме:
    ```bash
    docker compose up --build -d
    ```
3.  **Доступ к сервисам:**
* Демонстрационный UI: `http://localhost:8000`
* Документация API (Swagger): `http://localhost:8001/docs`
* Мониторинг Celery (Flower): `http://localhost:5555`
