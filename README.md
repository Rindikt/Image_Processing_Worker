🖼️ Асинхронный Обработчик Изображений (FastAPI + Celery + Redis)🎯 Ключевые достижения и Техническая АрхитектураЭтот проект демонстрирует реализацию асинхронной, масштабируемой системы обработки тяжелых задач, где веб-сервер (FastAPI) не блокируется длительными операциями, делегируя их фоновому обработчику (Celery Worker).1. Асинхронная Архитектура и Разделение ОтветственностиAPI (FastAPI): Обрабатывает входящие HTTP-запросы, мгновенно отправляет задачи в Celery и возвращает клиенту task_id для дальнейшего мониторинга.Worker (Celery): Независимый процесс, который потребляет задачи из брокера. Выполняет интенсивные операции, такие как обрезка, изменение размера и применение фильтров с помощью библиотеки Pillow (PIL).Брокер Задач: Redis используется как высокоскоростной брокер сообщений для Celery, обеспечивая надежную передачу и хранение статусов задач.2. Мульти-сервисная Контейнеризация (Docker)Проект оркестрован через Docker Compose и состоит из пяти независимых сервисов, демонстрируя полный цикл асинхронной системы:СервисТехнологияНазначениеapiFastAPI / PythonПрием запросов, диспетчеризация задач.workerCelery Worker / PythonВыполнение ресурсоемких задач обработки изображений.redisRedisБрокер сообщений для Celery и бэкенд для хранения результатов.flowerFlowerМониторинг и визуализация статуса задач Celery в реальном времени.frontendNginxОбслуживание статического HTML/JS интерфейса для демонстрации работы API.3. Функциональность APIAPI-сервер принимает изображение и параметры обрезки/фильтрации, немедленно возвращая идентификатор задачи.Асинхронные операции: /crop, /grayscale, /resize.Мониторинг: /task-status/{task_id} для проверки хода выполнения.Результат: /result/{task_id} для получения обработанного файла.4. Полный Конвейер CI/CD (GitHub Actions)Настроен полностью автоматизированный конвейер с использованием GitHub Actions для обеспечения качества кода и готовности к развертыванию:Job 'test': Автоматически запускает Pytest в контейнере api при каждом пуше. Использует фикстуры MagicMock для мокирования вызовов Celery, чтобы протестировать логику API без фактического запуска Celery.Job 'build_push': После успешного тестирования выполняет вход в Docker Hub, собирает единый образ приложения (используемый для api и worker) и публикует его в реестр Docker Hub.⚙️ Как запустить локальноДля запуска проекта достаточно наличия Docker и Docker Compose.Клонируйте репозиторий:Bashgit clone https://github.com/Rindikt/Image_Processing_Worker.git
cd Image_Processing_Worker
Соберите и запустите все сервисы в фоновом режиме:Bashdocker compose up --build -d
Доступ к сервисам:Демонстрационный UI: http://localhost:8000Документация API (Swagger): http://localhost:8001/docsМониторинг Celery (Flower): http://localhost:5555Запуск тестов:Bashdocker compose exec api pytest
